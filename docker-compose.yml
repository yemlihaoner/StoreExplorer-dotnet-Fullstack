services:
  stores-api:
    container_name: mymauiapp1-backend
    build:
      context: .
      dockerfile: Backend/Dockerfile
    ports:
      - "5271:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
